import {TEST_DATA_AND_RESPONSES} from "../data/moreData";
import {TEST_QUERIES} from "../data/exampleQueries";


const x = TEST_QUERIES.map((e, i) => [e, TEST_DATA_AND_RESPONSES[i]])

describe("endToEnd", () => {

  // build url from query
  // ?? hit the endpoint (using demo query) ??
  // response to data conversion
})